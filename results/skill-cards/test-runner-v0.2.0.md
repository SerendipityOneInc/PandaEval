# Skill Card: test-runner

> Evaluated on ClawHub skill `test-runner`

| Field | Value |
|-------|-------|
| **Skill** | test-runner |
| **Source** | [ClawHub](https://clawhub.com/skills/test-runner) |
| **Eval Date** | 2026-03-06 |
| **Eval Model** | claude-opus-4-6 |
| **Eval Engine** | skill-eval v0.2.0 |
| **Eval ID** | test-runner-2026-03-06-v0.2.0 |

---

## Overall Score: 6.5/10

## Summary

| Metric | With Skill | Without Skill | Delta |
|--------|-----------|--------------|-------|
| **Pass Rate** | 100% | 100% | 0% |
| **Avg Time** | 88.5s | 46.0s | +42.5s (+92%) |
| **Avg Tokens** | 3.8k | 2.6k | +1150 (+44%) |

## Test Cases

### write-python-tests

**Prompt:** I have a Python function that calculates compound interest. Can you write tests for it? Here's the function:

def compound_interest(principal, rate, years, n=12):
    return principal * (1 + rate/n) ** (n * years)

Save the function and tests so I can run them.

**With Skill:** 4/4 assertions passed

- [PASS] A pytest-compatible test file is created
- [PASS] Tests cover at least 3 different scenarios (normal case, zero values, edge cases)
- [PASS] Tests use assert statements or pytest assertions, not just print statements
- [PASS] The tests actually pass when run with pytest

**Without Skill:** 4/4 assertions passed

- [PASS] A pytest-compatible test file is created
- [PASS] Tests cover at least 3 different scenarios (normal case, zero values, edge cases)
- [PASS] Tests use assert statements or pytest assertions, not just print statements
- [PASS] The tests actually pass when run with pytest

**With Skill timing:** 62.0s | 2.6k tokens

**Without Skill timing:** 55.0s | 2.3k tokens


### write-react-tests

**Prompt:** I have this React component and I need tests for it. Write comprehensive tests.

```jsx
import React, { useState } from 'react';

export function Counter({ initialCount = 0, step = 1, max = 100 }) {
  const [count, setCount] = useState(initialCount);
  const increment = () => setCount(prev => Math.min(prev + step, max));
  const decrement = () => setCount(prev => Math.max(prev - step, 0));
  const reset = () => setCount(initialCount);
  
  return (
    <div>
      <span data-testid="count">{count}</span>
      <button onClick={increment}>+</button>
      <button onClick={decrement}>-</button>
      <button onClick={reset}>Reset</button>
    </div>
  );
}
```

**With Skill:** 4/4 assertions passed

- [PASS] Tests use @testing-library/react for rendering and interaction
- [PASS] Tests cover increment, decrement, and reset functionality
- [PASS] Tests check boundary conditions (max limit, zero floor)
- [PASS] Test file follows Arrange-Act-Assert pattern

**Without Skill:** 4/4 assertions passed

- [PASS] Tests use @testing-library/react for rendering and interaction
- [PASS] Tests cover increment, decrement, and reset functionality
- [PASS] Tests check boundary conditions (max limit, zero floor)
- [PASS] Test file follows Arrange-Act-Assert pattern

**With Skill timing:** 115.0s | 4.9k tokens

**Without Skill timing:** 37.0s | 2.9k tokens



## Strengths

- No clear strengths identified over baseline

## Weaknesses

- No measurable improvement over baseline (same pass rate)
- Slower than baseline (88.5s vs 46.0s)
- With-skill was significantly slower (92% more time) and used 44% more tokens.
- The skill adds project setup overhead but doesn't improve test quality for these prompts.

## Recommendation

Conditional. This skill shows some value but with trade-offs. Consider your use case.

---

## Eval Metadata

```json
{
  "eval_id": "test-runner-2026-03-06-v0.2.0",
  "skill_name": "test-runner",
  "skill_slug": "test-runner",
  "eval_date": "2026-03-06",
  "eval_model": "claude-opus-4-6",
  "skilleval_version": "0.2.0",
  "overall_score": 6.5,
  "with_skill_pass_rate": 1.0,
  "without_skill_pass_rate": 1.0,
  "evals_run": 2,
  "notes": [
    "Both configs produce high-quality pytest/RTL tests that pass. No pass-rate difference.",
    "With-skill version set up full project scaffolding (vitest config, package.json, setup.ts) for React tests \u2014 baseline just wrote the test file.",
    "With-skill was significantly slower (92% more time) and used 44% more tokens.",
    "The skill adds project setup overhead but doesn't improve test quality for these prompts.",
    "Non-discriminating assertions \u2014 need skill-specific assertions (e.g., does it scaffold the project? does it run tests automatically?)"
  ]
}
```

---

*Generated by skill-eval v0.2.0 | Model: claude-opus-4-6 | 2026-03-06*
